extends layout

block vars
	- var pageTitle = post.page_title
	- var pageClass = post.page_class + " update-single"
	
block content
	#hero(style="background-image: url(" + post.header_img + ");")
	
	#case
		h1.title= post.title

		article
			block post-content